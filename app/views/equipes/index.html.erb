<h1>Liste Equipes</h1>
<p>Visualiser nos équipes</p>

<table class="table table-striped table-bordered table-hover table-condensed">
  <tr>
		<th>Name</th>
    <th>Ville</th>
    <th></th>
    <th></th>
  </tr>
  <% @equipe.each do |f| %>
  <tr>
    <td><%= f.nom %></td>
		<td><%= f.ville %></td>
    <td><a href="<%= edit_equipe_path(f.id) %>" class="btn btn-primary">Editer</a></td>
    <td><a href="<%= delete_path(f.id) %>" class="btn btn-danger" data-method="DELETE">Supprimer</a></td>
	</tr>
  <% end %>
</table>
