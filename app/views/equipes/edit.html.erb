<h1>Equipes Edition</h1>
<p>Modifier une équipe</p>

<% if @equipe.invalid? %>
	<div class="alert-danger">

		<% @equipe.errors.full_messages.each do |msg| %>
			<%= msg %>
		<% end %>
	</div>
<% end %>

<%= form_for @equipe do |f| %>
  <%= f.label :nom %>:
  <%= f.text_field :nom, class: 'form-control' %><br />

  <%= f.label :ville %>:
  <%= f.text_field :ville, class: 'form-control' %><br />

  <%= f.submit %>
<% end %>
